def ArrayAdditionI(arr)

  # code goes here
  top = arr[0]
  arr.each do |a|
    if a > top
      top = a
    end
  end
  
  if arr.inject(:+) == 2*top
    arrte = true
  else
    arrte = false
  end
  return arrte
         
end
